# TaskBandit documentation site

This repository hosts the public TaskBandit documentation site for GitHub Pages.

The site is intentionally static and build-free. Edit the HTML/CSS files directly and GitHub Pages can serve them without a package manager or generated build output.
