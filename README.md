# kriziw.github.io documentation hub

This repository hosts the public documentation hub for kriziw projects on GitHub Pages.

The site is intentionally static and build-free. Edit the HTML/CSS files directly and GitHub Pages can serve them without a package manager or generated build output.

Project sections:

- `taskbandit/`
- `mindbuzz/`
- `vatsim-monitor/`
